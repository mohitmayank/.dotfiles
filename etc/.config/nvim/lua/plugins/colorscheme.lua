---@type LazySpec
return {
  {
    "maxmx03/solarized.nvim",
    lazy = false,
    priority = 1000,
    opts = {
      theme = "neo",
      variant = "winter", -- use "spring" | "summer" | "autumn" | "winter" for subtle tone shifts
    },
  },
}
